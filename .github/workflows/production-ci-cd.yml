# Optional / future: full GHCR + deploy. Authoritative checks are in ci.yaml (gitleaks, service matrix, web/mobile).
name: Production CI/CD (manual)

on:
  workflow_dispatch:

jobs:
  pointer:
    runs-on: ubuntu-latest
    steps:
      - name: Use Monorepo CI
        run: |
          echo "Run .github/workflows/ci.yaml on push/PR for tests, pip-audit, gitleaks, web-portal build."
          echo "Add image build/push and deploy jobs here when GHCR and target environment are ready."
