# PostgreSQL Host-Based Authentication Configuration
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections
local   all             all                                     trust
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# Docker network connections
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             10.0.0.0/8              scram-sha-256
host    all             all             192.168.0.0/16          scram-sha-256

# Replication connections
host    replication     replicator      172.16.0.0/12           scram-sha-256
host    replication     replicator      10.0.0.0/8              scram-sha-256
host    replication     replicator      192.168.0.0/16          scram-sha-256
host    replication     replicator      postgres-replica        scram-sha-256

# Application connections
host    db_user_profile user            172.16.0.0/12           scram-sha-256
host    db_user_profile user            10.0.0.0/8              scram-sha-256
host    db_user_profile user            192.168.0.0/16          scram-sha-256